# Buffer V12.1 当前设计系统

> **当前状态：V12.1 已发布，正式版本 v0.35.0。** 产品定义以 [`docs/V12_1_PRODUCT_CONTRACT.md`](docs/V12_1_PRODUCT_CONTRACT.md) 为最高权威。官网固定使用当前夜空视觉，不提供皮肤选择。

> 本机 CSV 账单观察是 2026-09-24 单独授权的私有试用例外，遵守 [`docs/product-design/BILL_IMPORT_PRIVATE_PILOT.md`](docs/product-design/BILL_IMPORT_PRIVATE_PILOT.md)。现在页仅在有本次账单时于原底部卡片位置切换观察；记录页分开呈现，不改变未来与条件。

> **当前阶段前端基线：** 以 [`site/index.src.html`](site/index.src.html) 为唯一可编辑视觉源码，以 [`site/index.html`](site/index.html) 为构建交付文件。后续前端优化必须在这套夜空全景、侧栏、四空间布局、字体、发光层级和克制动效上增量进行。根目录 Vite 页面、历史截图和其他工作区不是当前视觉基线。详细边界见 [`docs/CURRENT_FRONTEND_BASELINE.md`](docs/CURRENT_FRONTEND_BASELINE.md)。

## 设计命题

Buffer 是一张安静、可信、可以核对的现实时间地图。V11 的视觉命题是：**安静的纸面观察界面，图表是一块证据画布，不是一台控制终端。**

**缓冲区负责反映，不负责指导。** 设计的任务是降低理解成本，不是增加用户要完成的事情。

## Experience Compression

所有空间只使用三层信息：

- **Level 1 答案**：默认可见的唯一主问题。
- **Level 2 原因**：选择具体对象后出现的上下文说明。
- **Level 3 数据与编辑**：用户明确要求修改、核对、备份或查看全部时出现。

默认界面不得同时铺开三层。每页只有一个主要结论和一个主要操作；“现在”可以用紧凑事实组呈现更多已确认或可计算的信息，但不得用重复解释、空卡或未经确认的数据填充。

## 当前信息架构

- **现在**：当前余额、可支撑时间和保留边界是主答案；7/30/60/90 天余额、每日最低支出、近期已记录收支和下一笔已知变化使用同屏事实组。已记录的实际变化与按条件估算的未来变化分栏标注；缺少记录时不得假算实际日均支出或制造异常消费。
- **未来**：当前金额、范围末金额和边界日期构成三个直接答案；大型 Future Canvas 直接标注起点、终点与触及点；日期和事件使用相邻 Inspector，模拟使用不遮图的 Composer。
- **条件**：现金基础、固定收入、固定支出、未来一次事项的可读清单；维护与历史按需出现。
- **记录**：按日期组织的人类时间线；技术快照、预测记忆和备份属于展开内容。

设置、备份和技术说明必须保持次级，不得与四个空间竞争。

## 视觉真实性

1. baseline、scenario、reserve 和金额轴使用同一个线性 domain。
2. 现实、预计和模拟同时通过文字、线型与颜色区分。
3. 颜色不承担唯一语义。
4. 数值使用等宽数字，未知状态显示为未知而不是 0。
5. 图表点位说明使用引擎返回的 drivers，不由 UI 猜测。
6. 时间轴同时表达日期和相对今天的关系。

## 交互体系

- 默认表面只读，输入和编辑渐进披露。
- 桌面使用与 Canvas 固定相邻的窄上下文 Inspector，但默认不得占位或覆盖图表。
- 手机使用紧凑 Bottom Sheet 或专注态，核心画布必须仍可辨认。
- 临时表面支持初始焦点、Tab 循环、Escape、焦点归还与未保存确认。
- 点击图表日期只改变选中点与说明，不改变现实或预测。
- 到期事项只能由本人核对，日期经过不能自动写成现实。
- 多变化 Scenario 先展示人类可读摘要，关闭或保存都不得修改 Reality。
- What If 是显式模式；未开启时不显示 Scenario 线和 Composer，开启后现实图必须继续可见。
- 触控目标至少 44px，并避让手机 safe area。
- 动效只使用 transform 与 opacity，并尊重 `prefers-reduced-motion`。

## Reality Capture 体系

- Now 提供唯一主入口“现实有变化”，不得扩散为全站按钮墙。
- 桌面使用右侧相邻侧栏；手机使用不超过视口的 Bottom Sheet，并冻结背景滚动。
- 默认顺序固定为：已有到期事项、确认当前余额、说一句发生了什么、精确修改。
- 余额确认只有一个金额字段，不询问账户拆分或差额去向。
- 自然语言输入不是聊天界面，不出现头像、对话历史或建议；结果只显示“我理解为”的候选事实。
- 三项候选在 390 × 844 首屏仍能看到确认按钮；候选可改金额或移除，整批验证后一次写入。
- 语音只是自然语言输入方式，转写后仍经过候选与本人确认；权限拒绝或能力缺失时文字立即可用。
- 完成页只陈述“现实已更新”、当前余额和“未来已重新计算”，不鼓励、不评价。
- 复杂自然语言进入服务端解析时只显示“正在整理这句话里的现实变化……”，不出现头像、气泡、聊天历史或“AI 正在思考”。
- 部分成功时，明确候选可继续确认，模糊部分只问一个最小问题；失败时保留原文，并让余额确认、到期事项和精确修改继续可用。
- 三至五项候选在手机上使用紧凑事实卡；关键金额、日期和确认按钮优先，不形成长聊天线程。

## 单一视觉系统

官网固定使用当前夜空全景视觉。不得再展示皮肤选择入口或按皮肤切换材质、布局和配色。旧备份中的皮肤字段仅为兼容读取，不影响当前页面的视觉表达，也不改变事实、信息结构或计算。

## 组件规则

- **主按钮**：只用于用户主动发起的确认、保存或恢复。
- **次按钮**：用于查看解释、取消或低风险操作。
- **卡片**：只有需要独立交互或边界的事实组才使用卡片；优先留白和排版，不建立卡片墙或卡片套卡片。
- **输入**：必须有标签、单位、当前值和错误解释。
- **状态**：通过文字、图标和结构共同表达，不只依赖颜色。
- **浮层**：使用不透明表面，避免底层信息干扰阅读。

## 文案规则

- 先写事实，再写来源；先写结果，再按需展开计算过程。
- 普通界面使用“已确认”“预计”“待确认”“试算”“记录”等日常状态词。
- `REALITY`、`FORECAST`、`EXPECTED OCCURRENCE`、`RECONCILIATION`、`SNAPSHOT` 只属于代码、开发文档和按需技术详情。
- 用户界面不展示“区间末”“轨迹”“快照”“节点”“推演”等内部计算语言，改用具体的金额、日期、收支和结果说明。
- 不使用建议、催促、评价、人格推断或情绪操控。
- 不引入岗位、机会、项目、任务或产品范围外的身份语言。
- 不用宣传式标题、夸张承诺或金融终端术语制造权威感。

## 响应式与无障碍

- 桌面基准：1280×850。
- 手机基准：390×844，同时验证 320px 窄屏。
- 页面不得出现横向溢出。
- 满足 WCAG 2.2 AA 对比度和清晰焦点要求。
- 正文与解释文字使用 15 至 16px，标签与操作文字不小于 14px；只有日期、图表刻度等元数据可以使用 13px。
- 不使用 12px 承载用户需要阅读的内容。紧凑桌面布局通过减少重复文案、收紧间距和优化分栏维持一屏，不通过缩小文字维持。
- 一个事实只表达一次。标题不再配同义解释，字段名不再解释自身，导航只保留空间名称；仅保留计算假设、风险时间点、隐私和确认边界。
- “现在”页在超宽屏保持 1680px 以内的内容舞台；中央结论卡高度封顶，右侧摘要等高对齐，空的未来收支栏不占版面。
- 正文保持舒适行长和行距，压力状态下使用中性、具体语言。

## Historical / Superseded

早期阶段关于行动助手、个人管理、学习、成长角色和旧任务域的设计均已被 V10/V11/V12/V12.1 取代。V12.1 完成后停止，不得开始 OCR、银行同步、AI Chat、Final Hardening、V13 或新的产品功能。
